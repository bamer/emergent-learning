# Emergent Learning Framework - Core Dependencies
# Install with: pip install -r requirements.txt

# Database ORM (async)
peewee-aio[aiosqlite]>=2.0.0

# Async file I/O
aiofiles>=23.0.0

# Async HTTP client (for Ollama embeddings)
aiohttp>=3.8.0

# HTTP client (for OpenCode API) - upgraded for security
requests>=2.32.0

# Note: The dashboard has additional dependencies in dashboard-app/backend/requirements.txt

# YAML config parsing (for per-project config) - upgraded for security
PyYAML>=6.0.1